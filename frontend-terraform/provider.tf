# =============================================================================\n# AWS PROVIDER CONFIGURATION\n# =============================================================================\n\n# AWS Provider for NewsArchive Pro Frontend Infrastructure\n# Manages S3, CloudFront, and related services for React application deployment\nprovider "aws" {\n  region = var.aws_region  # Configurable AWS region (default: ap-southeast-2)\n  \n  # Default tags applied to all AWS resources\n  default_tags {\n    tags = {\n      Project     = \"NewsArchive Pro\"\n      Component   = \"Frontend Infrastructure\"\n      ManagedBy   = \"Terraform\"\n      Environment = var.environment\n    }\n  }\n}